# Tasks: add-project-readme

## 1. Content

- [x] 1.1 Write installation section (Homebrew tap + manual `swift build`)
- [x] 1.2 Write usage section (menu bar port list, filter, kill)
- [x] 1.3 Write development section (`Sources/Portero` layout, `Scripts/build-app.sh`, release workflow trigger)
- [x] 1.4 Write requirements/license section (macOS 13+, no `LICENSE` file disclosed honestly)

## 2. Verification

- [x] 2.1 Every command/path/name in the README cross-checked against the actual repo files (`Package.swift`, `Packaging/cask.rb.tmpl`, `.github/workflows/release.yml`, `Sources/Portero`)
- [x] 2.2 No fabricated license, badge, or link
